# URL of the running/deployed Kite web application. For a physical phone,
# use a reachable HTTPS origin or your computer's LAN address, not localhost.
EXPO_PUBLIC_API_BASE_URL=
# Optional separate web origin for user-initiated Privy sign-in and real trading.
EXPO_PUBLIC_WEB_URL=
